node_modules/
*.log
.DS_Store

# .claude-plugin/, .codex-plugin/, .generated/, and .opencode/ are the
# committed multi-runtime install surfaces. They are gitignored in the
# upstream monorepo (generated at install time) but committed here so a
# standalone clone installs into Claude Code, Codex, and OpenCode without
# any build step.
index.html
.hook-lib-vendor.json
